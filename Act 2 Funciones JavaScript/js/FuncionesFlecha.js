alert ('Conectado');
//calcular el perimetro de un circulo
const perimetroCirculoFlecha = (radio) => perimetroCir =radio*3.1415*2;

//calcular el area de un rectangulo
const areaRectanguloFlecha = (alturaFlecha, baseFlecha) => areaRec = baseFlecha*alturaFlecha;

//Cuadrado de un numero
const cuadradoFlecha = (numCuadradoFlecha) => cuadradoFlechaRes = numCuadradoFlecha*numCuadradoFlecha;

// Conversion de Centigrados a Farenheit
const conversionCentigradoFlecha = (temperaturaFlecha) => temperaturaResFlecha = (temperaturaFlecha * 1.8) + 32;

// Viva la reina isabel por ahí dicen los ingenieros 

const calcularVoltajeFlecha = (intensidad, resistencia) => voltajeFlecha= intensidad*resistencia;

// calcular el volumen de una esfera
const esferaVolumenFlecha = (radioEsferaFlecha) => esferaRes = (radioEsferaFlecha*3) * (3.1415) * (3/4);
